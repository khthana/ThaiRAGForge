"""rag_lab — modular indexing + retrieval experimentation framework.

Core package intentionally has no Streamlit dependency (ADR-0001): it is
importable and unit-testable on its own; UI and CLI are thin layers on top.
"""
