%% The prompt rules as they are actually sent to the model, verbatim Thai.
%%
%% NOT included by main.tex, because IEEEtran + pdflatex cannot typeset Thai.
%% To include it, compile with XeLaTeX and add to the preamble:
%%
%%     \usepackage{fontspec}
%%     \newfontfamily\thaifont{Noto Sans Thai}[Script=Thai]
%%
%% then \input{prompts_thai} where the figure should appear.
%%
%% Whether to show the Thai originals is a submission decision, not a
%% typesetting one: for a Thai NLP venue they are evidence, and the English
%% gloss in Table II is a translation the reader cannot check. Recommended:
%% include this as a figure and keep Table II as well.
%%
%% Source of truth: tools/eval/rq4_generate.py, `_RULE4` and
%% `build_instructions`. If that file changes, regenerate this one from it --
%% do not hand-edit, or the paper will quote a prompt the system never sent.

\begin{figure}[t]
\small
\begin{quote}
\thaifont
คุณคือผู้ช่วยตอบคำถามจากมติที่ประชุมสภาวิชาการ

กติกา:

1. ตอบจากเอกสารที่ให้มาเท่านั้น ห้ามใช้ความรู้อื่น

2. อ้างอิงเอกสารที่ใช้ด้วยหมายเลขในวงเล็บเหลี่ยม เช่น [1] หรือ [2], [5]

3. ถ้าเอกสารที่ให้มาไม่มีคำตอบ ให้ตอบว่า ไม่พบข้อมูล เท่านั้น ห้ามเดา

\medskip
\textrm{\textbf{Rule 4 --- \texttt{sentence\_cap} (baseline):}}

4. ตอบสั้น ๆ ไม่เกิน 3 ประโยค

\medskip
\textrm{\textbf{Rule 4 --- \texttt{cite\_all}:}}

4. อ้างอิงเอกสารที่เกี่ยวข้องทุกฉบับที่พบในเอกสารที่ให้มา ไม่ใช่แค่ฉบับเดียว
ความยาวคำตอบไม่จำกัด ตราบใดที่ครอบคลุมทุกฉบับที่เกี่ยวข้อง

\medskip
\textrm{\textbf{Rules 5--6 --- added by \texttt{cite\_all\_guarded}:}}

5. ถ้าไม่มีเอกสารประกอบมาให้เลย ให้ตอบว่า ไม่พบข้อมูล และอ้างอิงเป็น - เท่านั้น
ห้ามอ้างอิงหมายเลขใด ๆ ทั้งสิ้น \textbf{ข้อนี้สำคัญกว่าข้อ 4}

6. อ้างอิงได้เฉพาะหมายเลขที่ปรากฏจริงในเอกสารที่ให้มาข้างต้นเท่านั้น
ห้ามสร้างหมายเลขขึ้นเอง

\medskip
รูปแบบคำตอบ (ต้องมีสองบรรทัดนี้เสมอ):

คำตอบ: <คำตอบของคุณ>

อ้างอิง: [หมายเลข] หรือ - ถ้าไม่มี
\end{quote}
\caption{The instruction block, verbatim. The documents precede it and the
question follows it; see Section IV. Rule 3 is byte-identical across all three
variants.}
\label{fig:thai}
\end{figure}
