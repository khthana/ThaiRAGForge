# บันทึกการกระทบยอด Corpus (3 ก.ค. 2569) — งานที่ทำแล้ว และสิ่งที่ต้องทำต่อ

> **อัปเดต 5 ก.ค. 2569 (หมวด 3 ปิดแล้ว)**: ผู้ใช้หาไฟล์/ลิงก์ที่ขาดของ 18 กลุ่มมาไว้ใน
> `academic_resolutions/R2/` → OCR 8 PDF (สำเร็จทั้งหมด) → จับคู่**ด้วยเนื้อหา**ไม่ใช่เลข
> "กลุ่ม N" (เลขของผู้ใช้เหลื่อมกับเลขในรายงาน เช่น R2 "กลุ่ม 14" = กลุ่ม 13 ในรายงาน)
> แล้วดำเนินการ: วางไฟล์จริงทับไฟล์ที่ชื่อไม่ตรงเนื้อหา 6 กลุ่ม (G1/G4/G8/G13/G15/G16),
> แก้ URL รายไฟล์ 2 ไฟล์ (G5 อุตสาหกรรมอาหาร/เทคโนโลยีการเกษตร), park `.dup` 11 ไฟล์
> (รวม `_LINK.txt` ด้วย) สำหรับกลุ่มที่ชื่อโดน split/ซ้ำ (G2,3,6,7,9,10,11,12×2,14,17),
> และ park `รับรองรายงานการประชุม.md` ของ G18 (2568/7). R2 "กลุ่ม 2"(=1/2564 CEDT) และ
> "กลุ่ม 10" เป็นสำเนาซ้ำของไฟล์ที่มีอยู่แล้ว → ไม่ได้เพิ่ม
> **baseline ใหม่ (หลัง rebuild --apply):** master rows 2,374 / matched 2,352 /
> dup groups **18 → 2** / "ไม่มีไฟล์เลย" 2
> รายงานตัด **หมวด 1.1 และ หมวด 2** ออกถาวรตามที่ผู้ใช้แจ้ง (ไม่มี PDF ให้ตาม)
>
> **URL ปิดครบแล้ว (5 ก.ค.):**
> - G1 (2564/11 คณะวิศวกรรมศาสตร์) → `1y7S05oOvNyK6GhqBElGUAsE3uZn7H3wy` ✓
> - G4 (2564/5 เปิดรายวิชา) → **ไม่มี URL จริง ผู้ใช้สั่งตัด** → park `_LINK` ของไฟล์ 26 เป็น `.dup`
>   (เนื้อหา .md คงอยู่ครบ, ไฟล์ 27 เก็บ URL ของตัวเอง) → **dup groups = 0**
> - G8 → `1Ua0VUgcG5VRUH9HPxiyTXpCQlGWCbRek` · G13 → `1qv4zrWIQOp3vKy4VtV_e7a-hLAOiCKwf`
>   · G15 → `1W3pKQ3OP0vRdp7PM_IgW_vEH__-_IHcB` (แทน folder URL เดิม) ✓
> - G16 + G5 = ลิงก์ไฟล์ตรงครบแล้ว ✓
>
> **5 ก.ค. (รอบเก็บกวาดรายงาน — "ข้อ 2"):** เพิ่ม secondary matching ใน
> `rebuild_manifests.py` (docx ที่ key ไม่ตรงไฟล์ → จับด้วย ปี+ครั้ง+ชื่อเรื่อง,
> โน้ต "URL ใน docx ล้าสมัย") + เปลี่ยนไฟล์ที่มีจริงแต่ไม่อยู่ใน docx ให้เป็นสถานะ
> **"ครบ"** (เดิม "ครบ (ไม่มีใน docx)") ตามที่ผู้ใช้สั่ง "ไฟล์มีก็จับใส่เลย"
> **baseline สุดท้าย:** dup groups **0** / ครบ **2361** / ลิงก์ซ้ำ 6 (docx บรรทัดซ้ำ,
> ไฟล์มี+index แล้ว) / ค้างจริงที่ผู้ใช้ไม่ตามต่อ 6 = 4 ลิงก์หลุด + 1 (2568/8 URL เสีย)
> + 1 (รับรองฯ 2568/7 สั่งตัด). pytest ผ่าน
>
> **⚠️ G13 URL ยังผิด:** ลิงก์ที่ผู้ใช้ให้ (`1qv4zrW...`) เป็นของ **วิทยาลัยการจัดการ
> นวัตกรรมและอุตสาหการ** (มีในคลังแล้ว) ไม่ใช่ **วิทยาลัยเทคโนโลยีและนวัตกรรมวัสดุ**
> → คืนค่า G13 เป็น folder URL `1szeqEPS...` ไว้ก่อน รอ URL ไฟล์ที่ถูกจากผู้ใช้
> - ~~G18 CHECO~~ **ปิดแล้ว (5 ก.ค.):** ผู้ใช้ส่ง URL Drive มา → ดาวน์โหลด PDF ตรง
>   (`requests` + confirm-token → `R2/CHECO 7-2568.pdf`) → OCR → เขียนทับ
>   `2568/ครั้งที่ 7/...CHECO.md` (ยืนยันเนื้อ ครั้งที่ ๗/๒๕๖๘ CHECO) → ตั้ง `_LINK.txt`
>   = `1d4iz1dpnPweAn7pxBfxlvJf9IJZwIJFJ`. dup groups เหลือ 2 (G1, G4)
>
> **artifact docx ล้าสมัย (ไม่ใช่ไฟล์หาย) — 4 แถว "ไม่มีไฟล์เลย":** 2564/8 ×2 (G5) +
> 2568/7 ×2 (CHECO+รับรองฯ ที่ docx ยังถือ URL รวมเก่า). ไฟล์จริงมีครบ ลิงก์ถูกแล้ว
> โผล่เป็น "ครบ (ไม่มีใน docx)" แทน — master_list.csv คือตัวจริง ไม่ต้องตาม

> **อัปเดต 4 ก.ค. 2569**: ผู้ใช้ดาวน์โหลดไฟล์ที่ขาดครบแล้ว → OCR 54 ไฟล์ (สำเร็จทั้งหมด
> ผ่าน `ocr_pdf_to_md.py` ที่ปรับให้รับหลายโฟลเดอร์ + แปลง PDF ทีละหน้า) → ย้ายเข้า
> โฟลเดอร์การประชุมและกระทบยอดใหม่แล้ว **หมวด 1 และ 2 ปิดแล้ว** — baseline ใหม่:
> docx 2,364 / md 2,373 / matched 2,360 / "มีลิงก์แต่ไม่มี .md" เหลือ 1
> (2568/8 ชะลอการรับนักศึกษา — URL ต้นทางเสีย) / dup groups 18
> PDF ต้นฉบับเก็บเป็น archive ใน `academic_resolutions/missing/` และ `md-not-found/`
> ไฟล์ 4s-2566 `-111111` ตรวจแล้วเป็นสำเนาซ้ำของมติที่มีอยู่ → park เป็น `.dup`
> missing_report.md ปรับใหม่: หมวด 3 แสดงเนื้อหาจริงในไฟล์ + คำแนะนำรายกลุ่ม,
> หมวด 4 ใช้ชื่อเรื่องเต็มจากเนื้อเอกสาร เหลือค้าง: หมวด 1.1/2/3/4 ตามรายงาน

เอกสารนี้คือ handoff: เมื่อจัดการไฟล์ที่ขาดเสร็จแล้ว บอก Claude ให้ **อ่านไฟล์นี้แล้วทำต่อ**
ตามหัวข้อ "ขั้นตอนให้ Claude ทำต่อ" ด้านล่างได้เลย

## 1) งานที่ทำเสร็จแล้ว

กระทบยอด `1.docx` (วาระที่ capture ด้วยมือ) กับ `academic_resolutions/`
โดยจับคู่ด้วย Google Drive file ID — จับคู่ได้ 2,349 จาก ~2,360 รายการ

- **โครงสร้างโฟลเดอร์เป็นมาตรฐานเดียว**: `<ปี>/ครั้งที่ N` และ `<ปี>/ครั้งที่ Ns`
  (วาระพิเศษ) — เปลี่ยนชื่อ 15 โฟลเดอร์ที่เคยใช้ 3 รูปแบบปนกัน และแยก
  `2565/วาระพิเศษ ครั้งที่ 1_2565` (ติดป้ายผิด) เป็น `ครั้งที่ 2s` + `ครั้งที่ 3s`
- **`meeting_manifest.json` ทุกการประชุม (78 ไฟล์, 2,320 รายการ)** — จุดความจริงของ
  metadata: ชื่อเรื่องเต็ม (กู้จาก docx ได้ 2,306 รายการ; ชื่อไฟล์ถูกตัดที่ ~100
  ตัวอักษร 598 ไฟล์) + URL ลิงก์กลับ (ADR-0003)
- **Loader อ่าน manifest ก่อน** (`src/rag_lab/loaders/common.py`) และ session
  วาระพิเศษได้ค่า `"Ns"` ไม่ชนกับครั้งปกติแล้ว — เทสต์ผ่านทั้ง suite,
  build `dev_smoke` ยืนยัน index ใช้ชื่อเต็มแล้ว
- **ไฟล์ซ้ำ 9 กลุ่ม** (PDF เดียวกันถูกโหลดหลายไฟล์เพราะชื่อเรื่องขึ้นบรรทัดใหม่ตอน
  capture) ถูกเปลี่ยนนามสกุลเป็น `.dup` — ไม่เข้า index แต่กู้คืนได้
- **ผลลัพธ์**: `academic_resolutions/master_list.csv` (บัญชีแม่แทน 1.docx),
  `academic_resolutions/missing_report.md` (รายการที่ขาด),
  `tools/corpus_prep/rebuild_manifests.py` (รันกระทบยอดซ้ำได้)

## 2) สิ่งที่ผู้ใช้ต้องทำ (ตาม `academic_resolutions/missing_report.md`)

- [ ] **หมวด 1 — ไม่มีไฟล์เลย (11 รายการ)**: ดาวน์โหลด PDF จาก URL ในรายงาน แล้ว
  OCR ด้วย `tools/corpus_prep/ocr_pdf_to_md.py` วางไฟล์ `.md` + `<ชื่อ>_LINK.txt`
  ลงโฟลเดอร์การประชุมที่ระบุ
  (รายการ `2566 ครั้งที่ 5s` ที่ URL ลงท้าย `-Copy.pdf` น่าจะเป็นสำเนาของไฟล์ที่มี
  อยู่แล้ว — เช็คก่อนว่าซ้ำหรือไม่)
- [ ] **หมวด 2 — มีลิงก์แต่ไม่มี `.md` (43 รายการ)**: ดาวน์โหลด/OCR ใหม่;
  บาง URL ชี้ไปที่**โฟลเดอร์ Drive** หรือ URL เสีย (เช่น `http://เรื่อง ...`)
  ต้องเปิดหาไฟล์จริงเอง แล้วแก้ `_LINK.txt` ให้ชี้ไฟล์ที่ถูก
- [ ] **หมวด 3 — ลิงก์ซ้ำ/อาจผิดเรื่อง (18 กลุ่ม)**: ตรวจทีละกลุ่ม —
  ถ้า*เนื้อหาเหมือนกัน* แปลว่ามีเรื่องหนึ่งที่ยังไม่มีไฟล์จริง ให้หา URL ที่ถูกมา
  ดาวน์โหลด; ถ้า*เนื้อหาต่างกัน* แปลว่า `_LINK.txt` ของบางไฟล์ผิด ให้แก้ URL
- [ ] **หมวด 4 — ตกหล่นจาก 1.docx**: ถ้ายังจะใช้ 1.docx ให้เติมรายการเหล่านี้
  (รวมวาระพิเศษ 2565 ครั้งที่ 2s/3s ทั้งการประชุม) หรือเลิกใช้แล้วยึด
  `master_list.csv` แทน

## 3) ขั้นตอนให้ Claude ทำต่อ (หลังผู้ใช้ทำข้อ 2 เสร็จ)

1. รัน `python tools/corpus_prep/rebuild_manifests.py` (dry-run) เทียบตัวเลขกับ
   baseline ล่าสุด (3 ก.ค. 2569): docx 2,364 / md 2,320 / matched 2,349 /
   สถานะ "ไม่มีไฟล์เลย" 11 / "มีลิงก์แต่ไม่มี .md" 43 — ตัวเลขกลุ่มขาดควรลดลง
2. ถ้าดูสมเหตุสมผล รันด้วย `--apply` เพื่อเขียน manifest + master_list.csv +
   missing_report.md รอบใหม่
3. สุ่มตรวจ manifest ของโฟลเดอร์ที่มีไฟล์เพิ่ม ว่า title มาจาก docx (`title_source`)
   และ URL ถูกต้อง
4. รัน `.venv/Scripts/python.exe -m pytest` และ build `dev_smoke` ใหม่
   (index เก่าใน `data/index/` สร้างจาก resolution_id แบบเก่า — ลบ/build ทับ
   ก่อนเปรียบเทียบ metric ทุกครั้ง)
5. ถ้าผู้ใช้แก้หมวด 3 แล้ว: เช็คว่าไฟล์ `.dup` กลุ่มไหนควรลบถาวร และคู่ไฟล์
   เนื้อหาซ้ำ (เช่น `2568/ครั้งที่ 7` รับรองรายงานฯ กับ CHECO) แยกเนื้อหากันแล้ว
